#include<stdio.h>
#include<cs50.h>
#include<string.h>

int main(void)
{
    mesin sulam alis digital m001
    for ( int i = 0; i < strlen(s); i++)
    {
        if (s[i] >= 'a' && s[i] <= 'z' )
        {
            printf("%c", s[i] - 32);
        }
        else
        {
            printf("%c", s[i]);
        }
    }
    printf("\n");
}
